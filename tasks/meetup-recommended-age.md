# Meetup recommended age and birth-date assurance — 2026-09-19

Owner selected recommendations, not participation restrictions. Extend existing one-off and recurring meetup creation with optional inclusive minimum/maximum ages (whole years, 0–120). Blank means no recommendation. Show the range in cards, detail and application screens. Existing rooms remain unrestricted and all application/approval guards remain unchanged.

When the signed-in user has consented to the new purpose, compare their self-reported age privately on the application screen. Missing information, old-purpose consent and an out-of-range age never block joining. Names/date-of-birth are never included in public event metadata or applications. Explain birth-date use for the recommendation and retain the separate account/safety purpose of the optional name. Update the owner-only personal-info response with its consent version; accept old clients without treating old receipts as agreement to the new purpose.

Self-reporting never changes trust level. Phone possession alone does not prove a real name or date of birth. There is no identity-provider integration in this repository; display self-reported/unverified birth dates until an actual verified result can support a separate birth-date assurance signal. Provider selection is pending; no fabricated verification, credential collection, paid integration or document-upload workflow.

Implementation uses current room-preview JSON, the existing creation RPC and private personal-info storage. No new dependency or identity-verification schema. Follow existing React Native controls and BrandKit product tokens. Tests: existing Node runner (`npm test`), typecheck/lint, focused pgTAP for JSON validation, consent compatibility, no trust escalation and out-of-range/missing-DOB participation. No build/review submission included in this change.

## Verification and release state

- Implemented optional recommended ranges for both event types; cards/details/applications show them. No application or approval age gate. New-purpose consent is `2026-09-19`; old `2026-09-12` clients remain supported without upgrading their receipts. Self-reported dates stay unverified; phone verification wording no longer claims real-name proof.
- `npm test`: 183 passed. Typecheck, lint and `git diff --check` passed. Focused chilling/personal-info tests rerun after adding zero/null configuration coverage: 9 passed.
- Migration 0072 applied only to local `supabase_db_gling`, in one transaction with `ON_ERROR_STOP`. `supabase test db` on `meetup_recommended_age`, `chilling_free_flow`, `personal_info`: 113 assertions passed, including invalid-range rollback, old API preservation, no trust escalation, private information isolation and out-of-range/no-DOB applications.
- Local Expo web export passed. Orca browser QA found intrinsic input widths overflowing the row; `minWidth: 0` fixes it. Re-exported source verified at 320px (input bounds 22–148.5 and 156.5–283), with a 390px visual check as well. Screenshot: `/tmp/gling-age-create-320.png`. No physical-device QA claim.
- Production migration, web deployment and a new native build are not performed in this change. Build 31 remains the prior consent-flow build. Apply 0072 before publishing this client; `mobile-app` pushes automatically deploy the web app, so this commit remains local pending that coordinated release.
- Actual birth-date assurance still requires a selected identity provider and server-verified results bound to the account and date. Phone possession or editing a date must not fabricate assurance. If later allowing edits to a verified date, invalidate its verification or require re-verification. No provider credentials or contract were supplied during this task.
