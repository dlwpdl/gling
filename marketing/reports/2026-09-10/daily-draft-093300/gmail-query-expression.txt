JSON.stringify({title:document.title,url:location.href,range:Array.from(document.querySelectorAll('.Dj')).filter(x=>x.getClientRects().length).map(x=>x.innerText),emptyConfirmed:Array.from(document.querySelectorAll('[role=main]')).filter(x=>x.getClientRects().length).some(x=>x.innerText.includes('검색어와 일치하는 메일이 없습니다.')),rows:Array.from(document.querySelectorAll('tr.zA')).filter(x=>x.getClientRects().length).map(x=>({threadId:x.getAttribute('data-legacy-thread-id')||x.querySelector('[data-legacy-thread-id]')?.getAttribute('data-legacy-thread-id'),sender:x.querySelector('.bog')?.innerText.includes('Gling 이메일 전달 확인')?'내부 전달 테스트':x.querySelector('.yW')?.innerText,subject:x.querySelector('.bog')?.innerText.replace(/\b\d{4,8}\b/g,'[인증정보 숨김]'),date:x.querySelector('.xW [title]')?.getAttribute('title')||x.querySelector('.xW')?.innerText}))})